import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    dimblack: {
      main: "#0C0910",
    },
    black: {
      main: "#0C0C12",
    },
    blue: {
      main: "#3939AA",
    },
    yellow: {
      main: "#FFCF00",
    },
    white: {
      main: "#E9DCC1",
    },
  },
});

export default theme;
